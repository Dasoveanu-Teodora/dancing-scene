#pragma once

#include <string>

#include "core/gpu/mesh.h"
#include "utils/glm_utils.h"


namespace Tema3Object3D
{ 

	    




	//Mesh* CreateCone(const std::string& name, glm::vec3 Origin, float BaseRad, float height,float numberOfSides,glm::vec3 color);
	Mesh* CreateCone(const std::string& name, float nr);

}

